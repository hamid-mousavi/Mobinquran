# سؤالات باز (Open Questions)

مجموعهٔ مسائل نامشخص یا نیازمند تصمیم کاربر که پیش از بستن هر فاز باید روشن شوند.

## فاز ۳ — قرائت و مصحف

| # | مسئله | وضعیت |
| :--- | :--- | :--- |
| 1 | انتخاب دیتاست چیدمان ۱۵ خطی: quran-dev (رسمی، فاقد مرز کلمه) در برابر quran-qcf4 (MIT، دقیق). | **quran-qcf4 انتخاب شد**؛ بستهٔ ۶۰۴ صفحه ساخته؛ پروتوتایپ `?proto=mushaf` منتظر تأیید بصری. |
| 2 | معنی «حزب» در ناوبری: دادهٔ بسته `hizbQuarter` از ۱ تا ۲۴۰ دارد؛ حزب واقعی = هر ۴ ربع (quarterهای 1,5,9,...). پنل فعلی ۶۰ دکمه با quarterهای ۱..۶۰ را می‌نمایاند. | ثبت‌شده در `docs/QA_PHASE3_READING_MUSHAF.md` §۵؛ نیازمند تصمیم. |
| 3 | در ۴ صفحهٔ ۶۲، ۱۲۱، ۲۰۱، ۵۰۲ برچسب جزء/ربع‌حزب داده (alquran.cloud) وسط صفحه عوض می‌شود؛ سربرگ مصحف کدام برچسب را نشان دهد؟ | فعلاً برچسبِ اولین آیهٔ صفحه؛ قابل تغییر. |
| 4 | دادهٔ quran-qcf4: سرسورهٔ صفحهٔ ۲ با «االبَقَرَةِ» (ال مضاعف) ذخیره شده — در ادغام نهایی یا اصلاح املایی (نزدیک شدن به منبع اصلی) یا نمایش همان‌طور ثبت می‌شود. | نیازمند تصمیم در ادغام P3-T3. |
| 5 | P3-T8: متریال‌سازی chunking پیاده شد؛ اگر اندازه‌گیری روی دستگاه ضعیف همچنان لگ نشان داد، پنجره‌سازی واقعی با spacer. | منتظر اندازه‌گیری کاربر (`docs/QA_PHASE3_READING_MUSHAF.md` §۴). |

## فاز ۵ — صوت آفلاین و حفظ (P5-T1: تحقیق منابع — انجام‌شده)

| # | مسئله | وضعیت |
| :--- | :--- | :--- |
| 1 | **مجوز everyayah:** وب‌سایت مجوز سراسری منتشر نکرده؛ تنها متن صریح `timings_files/000_disclaimer.txt` است («© VerseByVerseQuran.com؛ برای استفاده از timingها لینک به سایت ضروری»). لینک مجوز منقضی شده؛ وضعیت صادقانه: «بدون مجوز رسمی، freely served». آینهٔ Maqra (Hugging Face) محتوای یکسان با SHA-256/انتساب درخواست می‌کند. | ثبت‌شده؛ برای پخش رایگان/غیرتجاری قابل دفاع، ولی برای انتشار عمومی باید در صفحهٔ «منابع و مجوزها» صادقانه نمایش داده شود. |
| 2 | **Menshawi_32kbps ناقص است:** صفحهٔ رسمی everyayah ۳۰۳ فایل غایب را نشان می‌دهد — **حل شد (P5-T1):** لیست دقیق ۳۰۲ آیه از `000_unverified_checksum.md5` استخراج و در `src/services/audioSources.ts` جاسازی شد؛ نمونه‌ها روی سرور 404 تأیید شدند. آیات غایب به‌صورت خودکار به آینهٔ کامل Maqra (128kbps) و CDN ارجاع می‌شوند. | **حل شد (P5-T1).** ۳۰۲ آیهٔ غایب؛ ۳ منبع fallback عملیاتی. |
| 3 | **CDN جایگزین alquran.cloud:** هر ۴ قاری به‌عنوان edition وجود دارند (`ar.parhizgar`، `ar.abdulbasitmurattal`، `ar.minshawi`، `ar.alafasy`) روی `cdn.islamic.network`. | **health-check انجام شد (P5-T1):** URL دقیق `cdn.islamic.network/quran/audio/{bitrate}/ar.{handle}/{globalVerseNumber}.mp3` — پرهیزگار ۴۸✅، عبدالباسط ۶۴✅، منشاوی ۱۲۸✅، عفاسی ۶۴✅. فقط دسترسی از ایران در QA کاربر می‌ماند. |
| 4 | **دسترسی از ایران:** دسترسی به `everyayah.com` و `cdn.islamic.network` توسط کاربر باید در مرورگر واقعی تست شود. | کاربر — در QA فاز ۵. |
| 5 | **الزامات پیوست ث/صدا:** کش آفلاین mp3 در SW نیازمند CORS/Range دارد؛ ADR-8 از قبل: Cache Storage + Range برای Safari. | **حل شد (P5-T2):** SW به injectManifest منتقل و Route صوت با `RangeRequestsPlugin` افزوده شد؛ hostهای قابل‌کش (ACAO `*`): everyayah و آینهٔ Maqra. **یافتهٔ جدید:** CDN islamic.network **بدون** ACAO است (preflight با Origin → 403)؛ پس فقط برای fallback آنلاین می‌ماند و قابل دانلود آفلاین نیست. |